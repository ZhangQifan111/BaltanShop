# 巴坦杂货铺 进货账本 — 系统架构设计

> 文档版本：v1.0
> 最后更新：2026-05-20
> 状态：**改造进行中**

---

## 一、项目概述

**项目目标：** 追踪日本软胶/玩具的进货成本、洗白（税费）计算、以及咸鱼卖出利润。

**技术栈：**
- 后端：Node.js + Express（ES6，无构建）
- 前端：单文件 SPA（`public/index.html`， vanilla JS， no framework）
- 数据库：SQLite（sql.js 内存数据库，文件持久化 `data.db`）
- OCR：Tesseract.js（日语+英语）
- 图片存储：本地文件系统 `public/images/`

**运行端口：** 4020
**仓库：** `git@github.com:ZhangQifan111/BaltanShop.git`

---

## 二、整体架构

```
浏览器 (SPA)
    │
    │ HTTP REST API
    ▼
server.js (Express)
    │
    ├── sql.js (内存 SQLite)
    │       │
    │       └── data.db (文件持久化，每次 write 后 saveDB())
    │
    ├── public/index.html (前端单文件，JS 内联)
    │       └── 静态图片: public/images/
    │
    └── Tesseract.js (截图 OCR 识别)
```

**请求流程：**
1. 浏览器发起 API 请求（GET/POST/PUT/DELETE）
2. Express 路由匹配（所有 `/api/*` 在静态文件服务前注册）
3. sql.js 操作内存数据库
4. `saveDB()` 将内存数据库写回 `data.db` 文件
5. 返回 JSON 响应

---

## 三、数据库设计

### 3.1 表总览

| 表名 | 用途 | 主键 |
|---|---|---|
| `toys` | 库存/已售商品主记录 | `id` |
| `purchase_records` | 进货在途记录（多阶段追踪） | `id` |
| `fee_rules` | 买入价估算费用规则 | `id` |
| `supplies` | 耗材库存（纸箱、包装） | `id` |
| `supply_logs` | 耗材变动日志 | `id` |
| `shipping_rules` | 国内快递运费规则 | `id` |
| `settings` | 键值配置（品类列表、默认利润率等） | `key` |

---

### 3.2 `toys` 表（库存/商品主记录）

**用途：** 所有商品的单一数据源，覆盖从入库到卖出的完整生命周期。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | INTEGER | 主键 |
| `date` | TEXT | 入库日期 |
| `name` | TEXT | 商品名称 |
| `category` | TEXT | 品类（软胶/手办/模型/盲盒/其他） |
| `source` | TEXT | 进货方式：`direct`（直购）/ `proxy`（代购）/ `domestic`（国内） |
| `cost` | REAL | 总成本（买入价+所有费用合计） |
| `status` | TEXT | 状态：`stock`（在库）/ `sold`（已发货）/ `done`（已完成）/ `returned`（退货） |
| `sell` | REAL | 卖出价 |
| `huabei` | REAL | 花呗/信用付款扣额 |
| `refund_amount` | REAL | 退款金额 |
| `extra_cost` | REAL | 额外成本 |
| `sold_date` | TEXT | 发货日期 |
| `done_date` | TEXT | 完成日期 |
| **— 买入成本分项（改造后入库时由 purchase_record 同步）—** |
| `japan_price_cny` | REAL | 阶段①购入价（日元折RMB） |
| `handling_fee` | REAL | 阶段①+② 手续费合计 |
| `japan_domestic_shipping` | REAL | 阶段①+② 日本国内运费合计 |
| `intl_shipping` | REAL | 阶段③国际运费 |
| `tax` | REAL | 阶段③税费 |
| **— 直购专用字段 —** |
| `exchange_rate` | REAL | 汇率（未使用） |
| `japan_price_jpy` | REAL | 日元价格（未使用） |
| **— 代购专用字段 —** |
| `proxy_price` | REAL | 代购价 |
| `proxy_intl_shipping` | REAL | 代购国际运费 |
| `proxy_domestic_shipping` | REAL | 代购国内运费 |
| **— 国内采购字段 —** |
| `domestic_price` | REAL | 国内拿货价 |
| `domestic_shipping` | REAL | 国内运费 |
| **— 物流相关 —** |
| `logistics_type` | TEXT | 快递方式（中通/顺丰） |
| `logistics_fee` | REAL | 快递费 |
| `logistics_tracking` | TEXT | 运单号 |
| `logistics_weight` | REAL | 重量(kg) |
| `logistics_region` | TEXT | 目的省份 |
| **— 包装材料 —** |
| `box_size` | TEXT | 纸箱规格（大/中/小纸箱） |
| `box_fee` | REAL | 纸箱费 |
| `packing_fee` | REAL | 包装费（气泡垫+胶带） |
| `toy_image_path` | TEXT | 商品图片路径（`images/xxx.jpg`） |

**历史数据问题：** 所有在 `fee_rules` 改造（2026-05-20）前入库的 toys 记录，其分项字段（`japan_price_cny` / `handling_fee` 等）均为 0，因为入库时这些字段未被写入。

---

### 3.3 `purchase_records` 表（进货在途）

**用途：** 追踪从下单到入库的全流程，分三个阶段记录。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | INTEGER | 主键 |
| `name` | TEXT | 商品名称 |
| `category` | TEXT | 品类 |
| `source` | TEXT | 进货方式（`direct`/`proxy`） |
| **阶段① 日本站** |||
| `stage1_date` | TEXT | 下单日期 |
| `stage1_amount` | REAL | 阶段①总金额（日元+手续费+日本运费） |
| `stage1_jpy` | REAL | 阶段①购入价（日元） |
| `stage1_handling` | REAL | 阶段①手续费 |
| `stage1_domestic_ship` | REAL | 阶段①日本国内运费 |
| `stage1_note` | TEXT | 备注 |
| **阶段② 日本仓库** |||
| `stage2_date` | TEXT | 到达仓库日期 |
| `stage2_amount` | REAL | 阶段②总金额（日本运费②+手续费②） |
| `stage2_domestic_ship` | REAL | 阶段②日本国内运费 |
| `stage2_handling` | REAL | 阶段②手续费 |
| `stage2_note` | TEXT | 备注 |
| **阶段③ 出境→入库** |||
| `stage3_date` | TEXT | 出库/入库日期 |
| `stage3_amount` | REAL | 阶段③总金额（国际运费+税费） |
| `stage3_intl_ship` | REAL | 国际运费 |
| `stage3_tax` | REAL | 海关税费 |
| `stage3_note` | TEXT | 备注 |
| **状态** |||
| `status` | TEXT | `stage1` / `stage2` / `stocked` / `cancelled` |
| `created_at` | TEXT | 创建时间 |

**流转规则：**
- 新建 → `status=stage1`（阶段①）
- 调用 `PUT /stage2` → `status=stage2`（阶段②）
- 调用 `PUT /stage3` → `status=stocked`，**同时在 toys 表创建入库记录**（阶段③触发入库）

**各阶段金额关系：**
```
stage1_amount = stage1_jpy + stage1_handling + stage1_domestic_ship
stage2_amount = stage2_domestic_ship + stage2_handling
stage3_amount = stage3_intl_ship + stage3_tax
toys.cost = stage1_amount + stage2_amount + stage3_amount
```

---

### 3.4 `fee_rules` 表（买入价估算费用规则）

**用途：** 配置各品类/箱规/进货方式的费用参数，供买入价计算器和利润计算使用。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | INTEGER | 主键 |
| `category` | TEXT | 品类（软胶/手办/模型...） |
| `box_size` | TEXT | 箱规（大纸箱/中纸箱/小纸箱，空=通用） |
| `purchase_type` | TEXT | `direct`（直购）/ `proxy`（代购） |
| `handling_fee_percent` | REAL | **咸鱼平台手续费%**（卖出时由咸鱼收取） |
| `japan_domestic_shipping` | REAL | 日本国内运费（固定） |
| `intl_shipping` | REAL | 国际运费（固定） |
| `tax` | REAL | ~~税费%（已废弃，用 customs_tax_percent）~~ |
| `domestic_shipping` | REAL | 国内运费（固定） |
| `box_fee` | REAL | 箱费（固定） |
| `packing_fee` | REAL | 包装费（固定） |
| `profit_margin_percent` | REAL | **默认利润率%**（用于买入价估算） |
| `proxy_fee` | REAL | 代购手续费（固定，¥） |
| `customs_tax_percent` | REAL | **海关税费%**（买价×%=税费，用于买入价估算） |
| `created_at` | TEXT | 创建时间 |
| `updated_at` | TEXT | 更新时间 |

**当前数据（示例）：**
```json
{ "category": "sofubi", "box_size": "大纸箱", "purchase_type": "direct",
  "handling_fee_percent": 1.6, "profit_margin_percent": 15, "customs_tax_percent": 13 }
```

---

### 3.5 `supplies` 表（耗材库存）

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | INTEGER | 主键 |
| `name` | TEXT | 耗材名称（大纸箱/中纸箱/小纸箱/气泡垫胶带） |
| `category` | TEXT | 类别（`box`/`packing`） |
| `unit_price` | REAL | 单价 |
| `stock` | INTEGER | 当前库存 |
| `unit` | TEXT | 单位（个/份） |
| `low_stock_threshold` | INTEGER | 低库存警告阈值 |

---

### 3.6 `shipping_rules` 表（快递运费规则）

按目的地分区配置首重+续重报价。

| 字段 | 类型 | 说明 |
|---|---|---|
| `region_name` | TEXT | 区域名（一区/二区/三区...） |
| `provinces` | TEXT | 省份列表（逗号分隔） |
| `first_weight` | REAL | 首重(kg) |
| `first_price` | REAL | 首重价格(¥) |
| `additional_weight` | REAL | 续重(kg) |
| `additional_price` | REAL | 续重价格(¥/kg) |

---

### 3.7 `settings` 表（键值配置）

| key | value 示例 | 说明 |
|---|---|---|
| `categories` | `软胶,模型,手办,盲盒,其他` | 品类列表 |
| `default_profit_margin` | `15` | 默认利润率% |

---

## 四、API 设计

### 4.1 RESTful 端点总览

**Toys（商品库存）**
| Method | Path | 说明 |
|---|---|---|
| GET | `/api/toys` | 列表（?status=stock&search=关键词） |
| POST | `/api/toys` | 新增（直接入库，不经过在途） |
| PUT | `/api/toys/:id` | 更新（含编辑弹窗所有字段） |
| DELETE | `/api/toys/:id` | 删除 |

**Purchase Records（进货在途）**
| Method | Path | 说明 |
|---|---|---|
| GET | `/api/purchase-records` | 列表（全部） |
| POST | `/api/purchase-records` | 新建阶段①记录 |
| PUT | `/api/purchase-records/:id` | 全量更新（编辑弹窗保存） |
| PUT | `/api/purchase-records/:id/stage1` | 更新阶段①分项（自动计算 stage1_amount） |
| PUT | `/api/purchase-records/:id/stage2` | 更新阶段②，推进至 stage2 |
| PUT | `/api/purchase-records/:id/stage3` | 更新阶段③，触发入库创建 toys 记录 |
| DELETE | `/api/purchase-records/:id` | 删除 |

**Fee Rules（费用规则）**
| Method | Path | 说明 |
|---|---|---|
| GET | `/api/fee-rules` | 列表 |
| POST | `/api/fee-rules` | 新建规则 |
| PUT | `/api/fee-rules/:id` | 更新规则 |
| DELETE | `/api/fee-rules/:id` | 删除 |

**买入价计算器**
| Method | Path | 说明 |
|---|---|---|
| POST | `/api/calc-buy-price` | 计算可接受的最高买入价 |

**Supplies（耗材）**
| Method | Path | 说明 |
|---|---|---|
| GET | `/api/supplies` | 列表 |
| POST | `/api/supplies` | 新增耗材 |
| PUT | `/api/supplies/:id` | 更新 |
| DELETE | `/api/supplies/:id` | 删除 |
| POST | `/api/supplies/:id/consume` | 消耗（发货时扣库存） |
| POST | `/api/supplies/:id/restock` | 补充库存 |

**Shipping（快递）**
| Method | Path | 说明 |
|---|---|---|
| GET | `/api/shipping-rules` | 列表 |
| POST | `/api/shipping-rules` | 新建规则 |
| PUT | `/api/shipping-rules/:id` | 更新 |
| DELETE | `/api/shipping-rules/:id` | 删除 |
| POST | `/api/shipping/calculate` | 计算运费 |

**其他**
| Method | Path | 说明 |
|---|---|---|
| GET | `/api/settings` | 获取所有配置 |
| PUT | `/api/settings/:key` | 更新单项配置 |
| GET | `/api/stats` | 仪表盘统计数据 |
| GET | `/api/categories` | 获取品类列表 |
| GET | `/api/box-sizes` | 获取箱规列表 |
| GET | `/api/supply-logs` | 耗材日志 |
| POST | `/api/recognize` | OCR 截图识别（日语订单截图） |
| POST | `/api/upload-toy-image/:id` | 上传商品图片 |
| DELETE | `/api/toy-image/:id` | 删除商品图片 |

---

## 五、买入价计算公式

**目标：** 已知咸鱼售价，反推可接受的最高「买入价」。

**费用按购入现金流顺序排列：**
```
日本站（日元） → 出境（国际运费） → 中国仓库（国内运费+箱费+包装费） → 咸鱼卖出（平台扣费）
```

**公式推导：**
```
sell = buy_price                    (买入价)
     + japan_domestic_shipping      (日本运费，固定)
     + proxy_fee                    (代购手续费，固定)
     + customs_tax                  (海关税费 = buy_price × customs_tax%)
     + intl_shipping                (国际运费，固定)
     + domestic_shipping            (国内运费，固定)
     + box_fee                      (箱费，固定)
     + packing_fee                  (包装费，固定)
     + xianyu_fee                   (咸鱼手续费 = sell × handling_fee%)
     + target_profit                (目标利润 = sell × profit_margin%)
```

**由于 `customs_tax = buy_price × customs_pct%`（买价出现在等式两边），需解方程：**
```
buy_price_max = (sell - total_fixed - xianyu_fee - target_profit) / (1 + customs_pct/100)
```

**其中：**
- `total_fixed = japan_domestic + proxy_fee + intl_shipping + domestic + box_fee + packing`
- `xianyu_fee = sell × handling_fee_percent / 100`
- `target_profit = sell × profit_margin_percent / 100`

---

## 六、前端页面结构

单文件 SPA，共 6 个 Tab，通过 `switchTab(n)` 切换。

### Tab 1: 首页（home）

**组件：**
- 仪表盘统计卡片（库存总值/在途成本/已售利润/完成率）
- 低库存提醒（耗材 stock < low_stock_threshold）
- 待发货列表（status=sold）
- 记一笔进货（新建在途记录表单，含截图识别按钮）

### Tab 2: 进货在途（transit）

**组件：**
- 搜索框（按名称过滤）
- 在途记录列表（卡片形式）
  - 每条显示：名称/品类/阶段进度 chip / 总金额
  - chip 逻辑：分项全为 0 时显示总额 `①¥474`，否则显示 `①买469费5运0`
  - 按钮：编辑 / 删除
- 编辑弹窗（openTransitEdit）
  - 阶段①：买入价（日元折RMB）/ 手续费 / 日本运费 → 自动计算小计
  - 阶段②：国内运费 / 手续费 → 自动计算小计
  - 阶段③：国际运费 / 税费 → 自动计算小计
  - 「推进到阶段②」/ 「推进到入库」按钮

### Tab 3: 卖出/完成（sold）

**组件：**
- 状态过滤器（全部/已发货/已完成/已退货）
- 搜索框
- 已售商品卡片列表
  - 每条显示：名称 / 成本 / 卖出价 / 利润（pf函数计算，**含咸鱼手续费扣减**）
  - 按钮：编辑 / 发货 / 完成后处理

**售后弹窗（openAfterSale）：**
- 全部退款 → status=done + refund记录
- 部分退款 → 填写金额
- 完成（无退款）→ status=done

### Tab 4: 耗材（supplies）

**组件：**
- 耗材库存列表（含低库存警告样式）
- 耗材日志（最近100条变动记录）
- 新增耗材按钮
- 补充库存弹窗

### Tab 5: 费用规则（feerules）

**组件：**
- fee_rules 表格（品类 / 箱规 / 进货方式 / 咸鱼手续费% / 利润率% / 海关税费%）
- 新建 / 编辑 / 删除规则
- 编辑弹窗表单（13个字段）

### Tab 6: 买入估算（calc）

**组件：**
- 咸鱼售价输入框
- 品类选择器
- 箱规选择器
- 进货方式（直购/代购）
- 自定义费率覆盖（可选）
- 计算结果展示（按购入顺序分四段）：
  - 日本站：运费 + 代购手续费 + **海关税费**（含计算说明）
  - 出境：国际运费
  - 中国仓库：国内运费 + 箱费 + 包装费
  - 咸鱼平台：手续费%（显示为卖出价×N%）
  - 底部：最高买入价 / 目标利润 / 盈亏平衡点

---

## 七、利润计算公式（pf 函数）

```javascript
function pf(r, xFee) {
  // xFee = 咸鱼手续费 = sell × handling_fee_percent / 100
  return sell
    - refund_amount       // 退款
    - huabei             // 花呗扣款
    - cost               // 总成本（买入价+费用合计）
    - logistics_fee       // 快递费
    - box_fee            // 纸箱费
    - packing_fee        // 包装费
    - xFee               // 咸鱼平台手续费（改造新增）
}
```

**注意：** `xFee` 由 `getXFee(toy)` 查 `fee_rules` 获取，按 `category + box_size + purchase_type` 匹配规则。

---

## 八、数据流向图

### 路径A：新建在途 → 入库（推荐流程）

```
用户填阶段①表单
    ↓ POST /api/purchase-records
purchase_records [stage1, stage1_jpy/handling/domestic_ship 已填充]
    ↓ 用户推进到阶段②
    PUT /api/purchase-records/:id/stage2
purchase_records [stage2, stage2_domestic_ship/handling 已填充]
    ↓ 用户推进到入库
    PUT /api/purchase-records/:id/stage3
    → 在 toys 表创建一条新记录
    → purchase_records.status = 'stocked'
toys [cost = stage1+stage2+stage3, 分项字段全部正确填充]
```

### 路径B：直接入库（少数情况）

```
用户填表单 → POST /api/toys
toys [分项字段由用户手动填写]
```

### 路径C：卖出

```
用户在售出弹窗填卖出价
    → confirmSell()
    → PUT /api/toys/:id (status='sold')
用户操作「完成后处理」
    → openAfterSale() → confirmPartialRefund() / confirmReturn() / resolve
    → PUT /api/toys/:id (status='done')
```

---

## 九、关键未解决问题（TODO）

> 以下问题需要在下一版明确后修复

### 9.1 历史数据分项为 0

**现状：** 所有在改造前入库的 toys 记录，`japan_price_cny` / `handling_fee` 等分项字段均为 0。

**影响：**
- 编辑货品弹窗打开时这些字段显示为空（因为 `0 || ''` = `''`）
- 已修复显示问题（用 `??` 替代 `||`）
- 但实际数据仍然是 0，无法自动恢复

**处理方式：** 用户需手动编辑补填各阶段费用，然后「保存」时会同步更新 toys 表分项字段。

### 9.2 阶段③税费计算逻辑不明确

**现状：** `stage3_tax` 由用户在编辑弹窗手动填写，无自动计算。

**问题：** `customs_tax_percent` 定义为「买价 × 百分比」，但在阶段③界面没有从 fee_rules 自动带入参考值，也没有按买入价自动重算。

**待确认：** 阶段③税费应该是用户手动填还是按公式自动算？

### 9.3 利润统计（GET /api/stats）未扣咸鱼手续费

**现状：** `/api/stats` 接口计算 `done` 状态玩具的利润时，直接用：
```
profit = sell - refund - cost - logistics_fee - box_fee - packing_fee
```
**没有扣咸鱼手续费**（`handling_fee_percent`）。

**影响：** 仪表盘显示的「总利润」和「利润率」比实际偏低。

**修复方向：** 在 `/api/stats` 的利润计算中加入 `getXFee()` 查手续费逻辑（需要先加载 fee_rules）。

### 9.4 老版本 `toys` 记录分项缺失同步

**现状：** 编辑在途记录（status=stocked）保存后，前端会尝试同步更新 toys 表分项字段。但有以下问题：

1. 同步逻辑按「名称 + 在库状态」查找 toys 记录，如果名称不完全匹配会失败
2. 同步时 `toys.cost` 被设为 `stage1_amount + stage2_amount + stage3_amount`（阶段总额），但 `japan_price_cny` 用的是 `stage1_jpy`，不是 `stage1_amount`

**修复方向：** 确认 `japan_price_cny` 应存 stage1_jpy（日元折算后）还是 stage1_amount（包含手续费等所有阶段①费用），并统一逻辑。

### 9.5 直购场景下 fee_rules 的 proxy_fee 未使用

**现状：** `proxy_fee`（代购手续费）在 fee_rules 中配置，但买入价计算器和前端 UI 均未使用该字段。

**原因：** 直购模式下没有「代购手续费」，该字段仅代购场景使用。但 calculator 界面没有区分直购/代购的费用展示差异。

### 9.6 `japan_price_jpy` / `exchange_rate` 字段未使用

**现状：** toys 表有 `japan_price_jpy`（日元价格）和 `exchange_rate` 字段，但整个系统没有使用它们。所有购入价均直接以人民币存储在 `japan_price_cny` 中。

---

## 十、前端 JS 全局变量

```javascript
var toys = [];         // 商品列表（从 GET /api/toys 加载）
var supplies = [];     // 耗材列表
var rules = [];        // 费用规则（从 GET /api/fee-rules 加载）
var categories = [];   // 品类列表
var sellId;            // 当前打开「售出」弹窗的商品 ID
var afterId;           // 当前打开「完成后处理」弹窗的商品 ID
var toyEditId;         // 当前编辑货品的 ID
var transitEditTargetId; // 当前编辑在途记录的 ID
var editingFeeRuleId;  // 当前编辑 fee rule 的 ID
```

---

## 十一、样式与主题

- **品牌名：** 巴坦杂货铺（Balitan Grocery / 巴尔坦商店）
- **主色调：** 深空灰 + 琥珀金点缀
- **Tab 数量：** 6 个
- **前端框架：** 无（vanilla JS）
- **字体：** Noto Serif SC（标题）+ 系统无衬线（正文）
- **移动端：** 支持（max-width 适配）

---

## 十二、部署与运维

**启动：**
```bash
cd /opt/buy-ledger
node server.js
# 服务自动监听 0.0.0.0:4020
```

**数据文件：**
- 数据库：`/opt/buy-ledger/data.db`（SQLite 文件，每次写入后自动持久化）
- 图片：`/opt/buy-ledger/public/images/`

**Git 远程：**
```
git@github.com:ZhangQifan111/BaltanShop.git
```

**近期提交历史（改造期间）：**
| 提交 | 说明 |
|---|---|
| `7954bfe` | fix: 新建在途记录 INSERT 漏 stage1 分项字段 |
| `5f3770b` | fix: 编辑保存智能同步 toys 表分项字段 |
| `1fad3e8` | fix: PUT /api/toys 补加 per-stage 字段；openToyEdit 修复 0 值显示 |
| `8f87927` | feat: 利润计算扣咸鱼手续费；loadData 加载 feeRules；售出弹窗显示手续费 |

---

*本文档随代码更新迭代，维护者：巴尔坦 & 奥特曼*
